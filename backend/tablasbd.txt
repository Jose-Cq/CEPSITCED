-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.pacientes (
  id_paciente uuid NOT NULL DEFAULT gen_random_uuid(),
  numero_hc character varying NOT NULL UNIQUE,
  dni character varying NOT NULL UNIQUE,
  genero character varying NOT NULL,
  fecha_nacimiento date NOT NULL,
  lugar_familia character varying,
  estado_civil character varying,
  grado_instruccion character varying,
  ocupacion character varying,
  direccion character varying,
  telefono character varying,
  correo character varying,
  created_at timestamp with time zone DEFAULT now(),
  nombres character varying,
  apellido_paterno character varying,
  apellido_materno character varying,
  pais character varying,
  departamento character varying,
  provincia character varying,
  distrito character varying,
  estado_cuenta character varying NOT NULL DEFAULT 'INDEPENDIENTE'::character varying CHECK (estado_cuenta::text = ANY (ARRAY['INDEPENDIENTE'::character varying, 'STANDBY'::character varying]::text[])),
  id_perfil_propio uuid,
  id_apoderado uuid,
  parentesco character varying,
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT pacientes_pkey PRIMARY KEY (id_paciente),
  CONSTRAINT pacientes_id_perfil_propio_fkey FOREIGN KEY (id_perfil_propio) REFERENCES public.perfiles(id),
  CONSTRAINT pacientes_id_apoderado_fkey FOREIGN KEY (id_apoderado) REFERENCES public.perfiles(id)
);
CREATE TABLE public.empleados (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  auth_id uuid UNIQUE,
  nombres character varying NOT NULL,
  correo character varying NOT NULL UNIQUE,
  dni character varying UNIQUE,
  telefono character varying,
  fecha_nacimiento date,
  rol_sistema character varying NOT NULL,
  tiempo_vigente character varying,
  activo boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  apellido_paterno character varying,
  apellido_materno character varying,
  fecha_inicio date,
  fecha_fin date,
  ofrece_servicios boolean DEFAULT true,
  CONSTRAINT empleados_pkey PRIMARY KEY (id),
  CONSTRAINT empleados_auth_id_fkey FOREIGN KEY (auth_id) REFERENCES auth.users(id)
);
CREATE TABLE public.citas (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  paciente_id uuid,
  psicologa_nombre character varying NOT NULL,
  servicio character varying NOT NULL,
  numero_sesion integer DEFAULT 1,
  fecha_cita date NOT NULL,
  hora_inicio time without time zone NOT NULL,
  hora_fin time without time zone NOT NULL,
  estado_cita character varying DEFAULT 'Pendiente'::character varying,
  estado_pago character varying DEFAULT 'Pendiente'::character varying,
  metodo_pago character varying,
  monto numeric,
  created_at timestamp with time zone DEFAULT now(),
  paquete_id uuid,
  comentario_paciente text,
  psicologo_id uuid,
  habitacion_id uuid,
  modalidad character varying NOT NULL DEFAULT 'Presencial'::character varying,
  link_reunion character varying,
  comentario_cambio_psicologo text,
  local_id uuid,
  tipo_pago character varying,
  promocion_aplicada boolean DEFAULT false,
  cupon_aplicado boolean DEFAULT false,
  recordatorio_enviado_en timestamp with time zone,
  CONSTRAINT citas_pkey PRIMARY KEY (id),
  CONSTRAINT citas_paciente_id_fkey FOREIGN KEY (paciente_id) REFERENCES public.pacientes(id_paciente),
  CONSTRAINT citas_paquete_id_fkey FOREIGN KEY (paquete_id) REFERENCES public.paquetes_adquiridos(id),
  CONSTRAINT citas_psicologo_id_fkey FOREIGN KEY (psicologo_id) REFERENCES public.empleados(id),
  CONSTRAINT citas_habitacion_fkey FOREIGN KEY (habitacion_id) REFERENCES public.habitaciones(id),
  CONSTRAINT citas_local_id_fkey FOREIGN KEY (local_id) REFERENCES public.locales(id)
);
CREATE TABLE public.servicios (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  nombre_servicio character varying NOT NULL UNIQUE,
  precio_sesion numeric,
  activo boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT timezone('utc'::text, now()),
  area_id uuid,
  local_id uuid,
  descripcion text,
  mostrar_landing boolean DEFAULT true,
  locales_ids ARRAY DEFAULT '{}'::uuid[],
  areas_ids ARRAY DEFAULT '{}'::uuid[],
  capacidad_maxima integer NOT NULL DEFAULT 1,
  promocion_activa boolean NOT NULL DEFAULT false,
  promo_descuento_porcentaje numeric,
  promo_fecha_inicio date,
  promo_fecha_fin date,
  cobro_por_hora boolean DEFAULT false,
  es_tramite boolean NOT NULL DEFAULT false,
  modalidades ARRAY NOT NULL DEFAULT '{Presencial,Virtual}'::character varying[],
  CONSTRAINT servicios_pkey PRIMARY KEY (id),
  CONSTRAINT servicios_area_fkey FOREIGN KEY (area_id) REFERENCES public.areas(id),
  CONSTRAINT servicios_local_fkey FOREIGN KEY (local_id) REFERENCES public.locales(id)
);
CREATE TABLE public.paquetes_catalogo (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  servicio_id uuid,
  nombre_paquete character varying NOT NULL,
  cantidad_sesiones integer NOT NULL,
  precio_total numeric NOT NULL,
  activo boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT timezone('utc'::text, now()),
  descripcion text,
  locales_ids ARRAY DEFAULT '{}'::uuid[],
  areas_ids ARRAY DEFAULT '{}'::uuid[],
  promocion_activa boolean NOT NULL DEFAULT false,
  capacidad_maxima integer DEFAULT 1,
  promo_descuento_porcentaje numeric,
  promo_fecha_inicio date,
  promo_fecha_fin date,
  cobro_por_hora boolean DEFAULT false,
  modalidades ARRAY NOT NULL DEFAULT '{Presencial,Virtual}'::character varying[],
  local_id uuid,
  CONSTRAINT paquetes_catalogo_pkey PRIMARY KEY (id),
  CONSTRAINT paquetes_catalogo_servicio_id_fkey FOREIGN KEY (servicio_id) REFERENCES public.servicios(id),
  CONSTRAINT paquetes_catalogo_local_id_fkey FOREIGN KEY (local_id) REFERENCES public.locales(id)
);
CREATE TABLE public.paquetes_adquiridos (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  paciente_id uuid,
  servicio_id uuid,
  paquete_catalogo_id uuid,
  nombre_paquete_snapshot character varying,
  sesiones_totales integer NOT NULL,
  sesiones_disponibles integer NOT NULL,
  monto_pagado numeric NOT NULL,
  metodo_pago character varying NOT NULL,
  created_at timestamp with time zone DEFAULT timezone('utc'::text, now()),
  CONSTRAINT paquetes_adquiridos_pkey PRIMARY KEY (id),
  CONSTRAINT paquetes_adquiridos_paciente_id_fkey FOREIGN KEY (paciente_id) REFERENCES public.pacientes(id_paciente),
  CONSTRAINT paquetes_adquiridos_servicio_id_fkey FOREIGN KEY (servicio_id) REFERENCES public.servicios(id),
  CONSTRAINT paquetes_adquiridos_paquete_catalogo_id_fkey FOREIGN KEY (paquete_catalogo_id) REFERENCES public.paquetes_catalogo(id)
);
CREATE TABLE public.tramites_documentales (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  paciente_id uuid,
  servicio_id uuid,
  tipo_documento character varying NOT NULL DEFAULT 'Informe Psicológico'::character varying,
  monto_pagado numeric DEFAULT 0,
  estado_pago character varying DEFAULT 'Pendiente'::character varying,
  modalidad_entrega character varying NOT NULL,
  fecha_recojo date,
  hora_recojo time without time zone,
  paquete_origen_id uuid,
  created_at timestamp with time zone DEFAULT timezone('utc'::text, now()),
  archivo_pdf text,
  habilitar_visualizacion boolean DEFAULT false,
  subido_por_id uuid,
  metodo_pago character varying DEFAULT 'Pendiente'::character varying,
  CONSTRAINT tramites_documentales_pkey PRIMARY KEY (id),
  CONSTRAINT tramites_documentales_paciente_id_fkey FOREIGN KEY (paciente_id) REFERENCES public.pacientes(id_paciente),
  CONSTRAINT tramites_documentales_servicio_id_fkey FOREIGN KEY (servicio_id) REFERENCES public.servicios(id),
  CONSTRAINT tramites_documentales_paquete_origen_id_fkey FOREIGN KEY (paquete_origen_id) REFERENCES public.paquetes_adquiridos(id),
  CONSTRAINT tramites_documentales_subido_por_id_fkey FOREIGN KEY (subido_por_id) REFERENCES public.empleados(id)
);
CREATE TABLE public.psicologo_servicio (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  psicologo_id uuid,
  servicio_id uuid,
  CONSTRAINT psicologo_servicio_pkey PRIMARY KEY (id),
  CONSTRAINT psicologo_servicio_psicologo_id_fkey FOREIGN KEY (psicologo_id) REFERENCES public.empleados(id),
  CONSTRAINT psicologo_servicio_servicio_id_fkey FOREIGN KEY (servicio_id) REFERENCES public.servicios(id)
);
CREATE TABLE public.horarios_empleados (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  empleado_id uuid NOT NULL,
  fecha date NOT NULL,
  hora_inicio time without time zone NOT NULL,
  hora_fin time without time zone NOT NULL,
  tipo text NOT NULL DEFAULT 'laboral'::text CHECK (tipo = ANY (ARRAY['laboral'::text, 'excepcion'::text, 'ausencia'::text, 'reprogramado'::text, 'reprogramado_origen'::text])),
  disponible boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  motivo text,
  hora_inicio_original time without time zone,
  hora_fin_original time without time zone,
  modalidad character varying NOT NULL DEFAULT 'Presencial'::character varying,
  local_id uuid,
  CONSTRAINT horarios_empleados_pkey PRIMARY KEY (id),
  CONSTRAINT horarios_empleados_empleado_id_fkey FOREIGN KEY (empleado_id) REFERENCES public.empleados(id),
  CONSTRAINT horarios_empleados_local_id_fkey FOREIGN KEY (local_id) REFERENCES public.locales(id)
);
CREATE TABLE public.perfiles (
  id uuid NOT NULL,
  dni character varying NOT NULL UNIQUE,
  nombres character varying NOT NULL,
  apellido_paterno character varying NOT NULL,
  apellido_materno character varying NOT NULL,
  fecha_nacimiento date NOT NULL,
  telefono character varying NOT NULL,
  correo character varying NOT NULL UNIQUE,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  activo boolean NOT NULL DEFAULT true,
  CONSTRAINT perfiles_pkey PRIMARY KEY (id),
  CONSTRAINT perfiles_id_fkey FOREIGN KEY (id) REFERENCES auth.users(id)
);
CREATE TABLE public.notas_recepcion (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  titulo character varying NOT NULL,
  motivo character varying,
  contenido text NOT NULL,
  fecha date NOT NULL DEFAULT CURRENT_DATE,
  hora time without time zone NOT NULL DEFAULT CURRENT_TIME,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  autor_nombre character varying,
  empleado_id uuid,
  CONSTRAINT notas_recepcion_pkey PRIMARY KEY (id),
  CONSTRAINT notas_recepcion_empleado_id_fkey FOREIGN KEY (empleado_id) REFERENCES public.empleados(id)
);
CREATE TABLE public.locales (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  nombre character varying NOT NULL UNIQUE,
  direccion text,
  activo boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT timezone('utc'::text, now()),
  CONSTRAINT locales_pkey PRIMARY KEY (id)
);
CREATE TABLE public.areas (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  nombre character varying NOT NULL UNIQUE,
  activo boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT timezone('utc'::text, now()),
  CONSTRAINT areas_pkey PRIMARY KEY (id)
);
CREATE TABLE public.cargos (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  nombre character varying NOT NULL UNIQUE,
  nivel_acceso integer DEFAULT 1,
  activo boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT timezone('utc'::text, now()),
  CONSTRAINT cargos_pkey PRIMARY KEY (id)
);
CREATE TABLE public.asignaciones_empleado (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  empleado_id uuid NOT NULL,
  local_id uuid NOT NULL,
  area_id uuid NOT NULL,
  cargo_id uuid NOT NULL,
  created_at timestamp with time zone DEFAULT timezone('utc'::text, now()),
  CONSTRAINT asignaciones_empleado_pkey PRIMARY KEY (id),
  CONSTRAINT asign_empleado_fkey FOREIGN KEY (empleado_id) REFERENCES public.empleados(id),
  CONSTRAINT asign_local_fkey FOREIGN KEY (local_id) REFERENCES public.locales(id),
  CONSTRAINT asign_area_fkey FOREIGN KEY (area_id) REFERENCES public.areas(id),
  CONSTRAINT asign_cargo_fkey FOREIGN KEY (cargo_id) REFERENCES public.cargos(id)
);
CREATE TABLE public.informes_mensuales (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  empleado_id uuid NOT NULL,
  archivo_url text NOT NULL,
  mes integer NOT NULL CHECK (mes >= 1 AND mes <= 12),
  anio integer NOT NULL,
  estado character varying DEFAULT 'Enviado'::character varying CHECK (estado::text = ANY (ARRAY['Enviado'::character varying::text, 'Revisado'::character varying::text, 'Observado'::character varying::text])),
  created_at timestamp with time zone DEFAULT timezone('utc'::text, now()),
  updated_at timestamp with time zone DEFAULT timezone('utc'::text, now()),
  plazo_id uuid,
  archivo_nombre character varying,
  CONSTRAINT informes_mensuales_pkey PRIMARY KEY (id),
  CONSTRAINT informes_empleado_fkey FOREIGN KEY (empleado_id) REFERENCES public.empleados(id),
  CONSTRAINT informes_mensuales_plazo_id_fkey FOREIGN KEY (plazo_id) REFERENCES public.plazos_informes_mensuales(id)
);
CREATE TABLE public.actividades_empleado (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  empleado_id uuid NOT NULL,
  fecha date NOT NULL,
  hora_inicio time without time zone NOT NULL,
  hora_fin time without time zone NOT NULL,
  tarea character varying NOT NULL,
  descripcion text,
  estado character varying DEFAULT 'Programado'::character varying,
  created_at timestamp with time zone DEFAULT timezone('utc'::text, now()),
  local_id uuid,
  grupo_actividad_id text,
  CONSTRAINT actividades_empleado_pkey PRIMARY KEY (id),
  CONSTRAINT act_empleado_fkey FOREIGN KEY (empleado_id) REFERENCES public.empleados(id),
  CONSTRAINT actividades_empleado_local_id_fkey FOREIGN KEY (local_id) REFERENCES public.locales(id)
);
CREATE TABLE public.habitaciones (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  local_id uuid NOT NULL,
  nombre character varying NOT NULL,
  activo boolean DEFAULT true,
  CONSTRAINT habitaciones_pkey PRIMARY KEY (id),
  CONSTRAINT habitaciones_local_fkey FOREIGN KEY (local_id) REFERENCES public.locales(id)
);
CREATE TABLE public.asistencias_empleados (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  empleado_id uuid NOT NULL,
  fecha date NOT NULL DEFAULT CURRENT_DATE,
  hora_entrada time without time zone,
  hora_salida time without time zone,
  estado character varying NOT NULL DEFAULT 'Presente'::character varying,
  justificacion text,
  created_at timestamp with time zone DEFAULT now(),
  evidencia text,
  CONSTRAINT asistencias_empleados_pkey PRIMARY KEY (id),
  CONSTRAINT asistencias_empleado_fkey FOREIGN KEY (empleado_id) REFERENCES public.empleados(id)
);
CREATE TABLE public.solicitudes_reprogramacion (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  cita_id uuid,
  actividad_id uuid,
  empleado_id uuid NOT NULL,
  tipo character varying NOT NULL,
  fecha_original date,
  hora_inicio_original time without time zone,
  hora_fin_original time without time zone,
  nueva_fecha date NOT NULL,
  nueva_hora_inicio time without time zone NOT NULL,
  nueva_hora_fin time without time zone NOT NULL,
  motivo text,
  estado character varying NOT NULL DEFAULT 'Pendiente'::character varying,
  created_at timestamp with time zone DEFAULT now(),
  archivo_url text,
  CONSTRAINT solicitudes_reprogramacion_pkey PRIMARY KEY (id),
  CONSTRAINT solicitudes_reprogramacion_cita_fkey FOREIGN KEY (cita_id) REFERENCES public.citas(id),
  CONSTRAINT solicitudes_reprogramacion_actividad_fkey FOREIGN KEY (actividad_id) REFERENCES public.actividades_empleado(id),
  CONSTRAINT solicitudes_reprogramacion_empleado_fkey FOREIGN KEY (empleado_id) REFERENCES public.empleados(id)
);
CREATE TABLE public.metodos_pago_clinica (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  tipo character varying NOT NULL CHECK (tipo::text = ANY (ARRAY['TRANSFERENCIA'::character varying, 'YAPE'::character varying]::text[])),
  banco character varying,
  moneda character varying DEFAULT 'Soles'::character varying,
  numero_cuenta character varying,
  cci character varying,
  titular character varying NOT NULL,
  numero_yape character varying,
  qr_url text,
  telefono_confirmacion character varying,
  mensaje_confirmacion text,
  activo boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT metodos_pago_clinica_pkey PRIMARY KEY (id)
);
CREATE TABLE public.landing_carousel (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  titulo character varying NOT NULL,
  subtitulo text,
  descripcion text,
  imagen_url text,
  orden integer DEFAULT 1,
  activo boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT landing_carousel_pkey PRIMARY KEY (id)
);
CREATE TABLE public.feriados (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  nombre character varying NOT NULL,
  fecha date NOT NULL UNIQUE,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT feriados_pkey PRIMARY KEY (id)
);
CREATE TABLE public.reglas_precios (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  servicio_id uuid,
  paquete_catalogo_id uuid,
  local_id uuid,
  cargo_id uuid,
  precio numeric,
  descuento_porcentaje numeric,
  promo_fecha_inicio date,
  promo_fecha_fin date,
  created_at timestamp with time zone DEFAULT timezone('utc'::text, now()),
  grupo_id text,
  CONSTRAINT reglas_precios_pkey PRIMARY KEY (id),
  CONSTRAINT reglas_precios_servicio_id_fkey FOREIGN KEY (servicio_id) REFERENCES public.servicios(id),
  CONSTRAINT reglas_precios_paquete_catalogo_id_fkey FOREIGN KEY (paquete_catalogo_id) REFERENCES public.paquetes_catalogo(id),
  CONSTRAINT reglas_precios_local_id_fkey FOREIGN KEY (local_id) REFERENCES public.locales(id),
  CONSTRAINT reglas_precios_cargo_id_fkey FOREIGN KEY (cargo_id) REFERENCES public.cargos(id)
);
CREATE TABLE public.landing_personal (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  empleado_id uuid NOT NULL,
  nro_cpsp character varying,
  horario character varying,
  modalidad character varying,
  imagen_perfil_url text,
  perfil_profesional text,
  formacion_1 character varying,
  formacion_2 character varying,
  formacion_3 character varying,
  formacion_4 character varying,
  orden integer DEFAULT 1,
  activo boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT landing_personal_pkey PRIMARY KEY (id),
  CONSTRAINT landing_personal_empleado_fkey FOREIGN KEY (empleado_id) REFERENCES public.empleados(id)
);
CREATE TABLE public.landing_testimonios (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  nombre_ficticio character varying NOT NULL,
  comentario text NOT NULL,
  calificacion integer NOT NULL CHECK (calificacion >= 1 AND calificacion <= 5),
  orden integer DEFAULT 1,
  activo boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT landing_testimonios_pkey PRIMARY KEY (id)
);
CREATE TABLE public.historias_clinicas (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  paciente_id uuid NOT NULL,
  servicio character varying NOT NULL,
  version integer NOT NULL DEFAULT 1,
  sesion_numero integer NOT NULL DEFAULT 1,
  fecha_intervencion date NOT NULL DEFAULT CURRENT_DATE,
  lugar_tiempo text,
  hijos_hermanos text,
  religion character varying,
  acompanante character varying,
  correo character varying,
  historia_personal text,
  diagnostico_presuntivo text,
  recomendaciones text,
  created_at timestamp with time zone DEFAULT now(),
  hora_intervencion character varying,
  sede character varying,
  consultorio character varying,
  cita_id uuid,
  psicologo_id uuid,
  CONSTRAINT historias_clinicas_pkey PRIMARY KEY (id),
  CONSTRAINT historias_clinicas_paciente_id_fkey FOREIGN KEY (paciente_id) REFERENCES public.pacientes(id_paciente),
  CONSTRAINT historias_clinicas_cita_id_fkey FOREIGN KEY (cita_id) REFERENCES public.citas(id),
  CONSTRAINT historias_clinicas_psicologo_id_fkey FOREIGN KEY (psicologo_id) REFERENCES public.empleados(id)
);
CREATE TABLE public.cita_historia_relacion (
  cita_id uuid NOT NULL,
  historia_clinica_id uuid NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT cita_historia_relacion_pkey PRIMARY KEY (cita_id, historia_clinica_id),
  CONSTRAINT cita_historia_relacion_cita_id_fkey FOREIGN KEY (cita_id) REFERENCES public.citas(id),
  CONSTRAINT cita_historia_relacion_historia_clinica_id_fkey FOREIGN KEY (historia_clinica_id) REFERENCES public.historias_clinicas(id)
);
CREATE TABLE public.permisos_pacientes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  paciente_id uuid NOT NULL,
  paciente_nombre character varying NOT NULL,
  solicitante_id uuid NOT NULL,
  solicitante_nombre character varying NOT NULL,
  propietario_id uuid NOT NULL,
  propietario_nombre character varying NOT NULL,
  estado character varying NOT NULL DEFAULT 'pendiente'::character varying CHECK (estado::text = ANY (ARRAY['pendiente'::character varying::text, 'aprobado'::character varying::text, 'denegado'::character varying::text])),
  duracion_horas integer,
  fecha_expiracion timestamp with time zone,
  created_at timestamp with time zone DEFAULT now(),
  servicio character varying,
  CONSTRAINT permisos_pacientes_pkey PRIMARY KEY (id),
  CONSTRAINT permisos_pacientes_paciente_id_fkey FOREIGN KEY (paciente_id) REFERENCES public.pacientes(id_paciente),
  CONSTRAINT permisos_pacientes_solicitante_id_fkey FOREIGN KEY (solicitante_id) REFERENCES public.empleados(id),
  CONSTRAINT permisos_pacientes_propietario_id_fkey FOREIGN KEY (propietario_id) REFERENCES public.empleados(id)
);
CREATE TABLE public.bitacoras_sesiones (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  cita_id uuid NOT NULL UNIQUE,
  bitacora text NOT NULL,
  archivo_pdf text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT bitacoras_sesiones_pkey PRIMARY KEY (id),
  CONSTRAINT bitacoras_sesiones_cita_id_fkey FOREIGN KEY (cita_id) REFERENCES public.citas(id)
);
CREATE TABLE public.landing_faq (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  pregunta text NOT NULL,
  respuesta text NOT NULL,
  orden integer DEFAULT 1,
  activo boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT landing_faq_pkey PRIMARY KEY (id)
);
CREATE TABLE public.asistencia_detalle (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  asistencia_id uuid NOT NULL,
  turno_index integer NOT NULL,
  hora_inicio time without time zone,
  hora_fin time without time zone,
  hora_entrada time without time zone,
  hora_salida time without time zone,
  estado character varying NOT NULL DEFAULT 'Pendiente'::character varying,
  tipo_justificacion character varying,
  justificacion text,
  evidencia text,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT asistencia_detalle_pkey PRIMARY KEY (id),
  CONSTRAINT asistencia_detalle_asistencia_id_fkey FOREIGN KEY (asistencia_id) REFERENCES public.asistencias_empleados(id)
);
CREATE TABLE public.cupones (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  codigo character varying NOT NULL UNIQUE,
  tipo_descuento character varying NOT NULL CHECK (tipo_descuento::text = ANY (ARRAY['Porcentaje'::character varying::text, 'Monto'::character varying::text])),
  valor_descuento numeric NOT NULL CHECK (valor_descuento >= 0::numeric),
  tipo_cupon character varying NOT NULL DEFAULT 'Paciente'::character varying CHECK (tipo_cupon::text = ANY (ARRAY['Paciente'::character varying::text, 'Empresa'::character varying::text])),
  empresa_nombre character varying,
  servicio_id uuid,
  paquete_catalogo_id uuid,
  cantidad_usos_maximo integer,
  cantidad_usos_actual integer NOT NULL DEFAULT 0,
  fecha_inicio date,
  fecha_fin date,
  un_uso_por_paciente boolean NOT NULL DEFAULT true,
  activo boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT cupones_pkey PRIMARY KEY (id),
  CONSTRAINT cupones_servicio_id_fkey FOREIGN KEY (servicio_id) REFERENCES public.servicios(id),
  CONSTRAINT cupones_paquete_catalogo_id_fkey FOREIGN KEY (paquete_catalogo_id) REFERENCES public.paquetes_catalogo(id)
);
CREATE TABLE public.cupones_usos (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  cupon_id uuid NOT NULL,
  paciente_id uuid NOT NULL,
  cita_id uuid,
  paquete_adquirido_id uuid,
  fecha_uso timestamp with time zone DEFAULT now(),
  CONSTRAINT cupones_usos_pkey PRIMARY KEY (id),
  CONSTRAINT cupones_usos_cupon_id_fkey FOREIGN KEY (cupon_id) REFERENCES public.cupones(id),
  CONSTRAINT cupones_usos_paciente_id_fkey FOREIGN KEY (paciente_id) REFERENCES public.pacientes(id_paciente),
  CONSTRAINT cupones_usos_cita_id_fkey FOREIGN KEY (cita_id) REFERENCES public.citas(id),
  CONSTRAINT cupones_usos_paquete_adquirido_id_fkey FOREIGN KEY (paquete_adquirido_id) REFERENCES public.paquetes_adquiridos(id)
);
CREATE TABLE public.cita_pacientes_adicionales (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  cita_id uuid NOT NULL,
  paciente_id uuid NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT cita_pacientes_adicionales_pkey PRIMARY KEY (id),
  CONSTRAINT cita_pacientes_adicionales_cita_id_fkey FOREIGN KEY (cita_id) REFERENCES public.citas(id),
  CONSTRAINT cita_pacientes_adicionales_paciente_id_fkey FOREIGN KEY (paciente_id) REFERENCES public.pacientes(id_paciente)
);
CREATE TABLE public.empresas (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  nombre character varying NOT NULL UNIQUE,
  ruc character varying UNIQUE,
  direccion text,
  telefono character varying,
  correo character varying,
  contacto_nombre character varying,
  activo boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT empresas_pkey PRIMARY KEY (id)
);
CREATE TABLE public.empresa_pacientes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  empresa_id uuid NOT NULL,
  paciente_id uuid NOT NULL,
  cargo character varying,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT empresa_pacientes_pkey PRIMARY KEY (id),
  CONSTRAINT empresa_pacientes_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.empresas(id),
  CONSTRAINT empresa_pacientes_paciente_id_fkey FOREIGN KEY (paciente_id) REFERENCES public.pacientes(id_paciente)
);
CREATE TABLE public.plazos_informes_mensuales (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  titulo character varying NOT NULL,
  fecha_limite date NOT NULL,
  hora_limite time without time zone NOT NULL DEFAULT '23:59:00'::time without time zone,
  descripcion text,
  cerrado boolean DEFAULT false,
  created_at timestamp with time zone DEFAULT timezone('utc'::text, now()),
  updated_at timestamp with time zone DEFAULT timezone('utc'::text, now()),
  CONSTRAINT plazos_informes_mensuales_pkey PRIMARY KEY (id)
);
CREATE TABLE public.encuestas (
  id bigint NOT NULL DEFAULT nextval('encuestas_id_seq'::regclass),
  created_at timestamp with time zone DEFAULT now(),
  titulo text NOT NULL,
  instrucciones text,
  fecha_limite date NOT NULL,
  hora_limite time without time zone DEFAULT '23:59:00'::time without time zone,
  link_google_forms text,
  plantilla_url text,
  plantilla_nombre text,
  creado_por uuid,
  cerrada boolean DEFAULT false,
  CONSTRAINT encuestas_pkey PRIMARY KEY (id),
  CONSTRAINT encuestas_creado_por_fkey FOREIGN KEY (creado_por) REFERENCES public.empleados(id)
);
CREATE TABLE public.encuesta_respuestas (
  id bigint NOT NULL DEFAULT nextval('encuesta_respuestas_id_seq'::regclass),
  created_at timestamp with time zone DEFAULT now(),
  encuesta_id bigint NOT NULL,
  empleado_id uuid NOT NULL,
  respondido boolean DEFAULT false,
  archivo_respuesta_url text,
  archivo_respuesta_nombre text,
  responded_at timestamp with time zone,
  CONSTRAINT encuesta_respuestas_pkey PRIMARY KEY (id),
  CONSTRAINT encuesta_respuestas_encuesta_id_fkey FOREIGN KEY (encuesta_id) REFERENCES public.encuestas(id),
  CONSTRAINT encuesta_respuestas_empleado_id_fkey FOREIGN KEY (empleado_id) REFERENCES public.empleados(id)
);
CREATE TABLE public.catalogo_pruebas (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  nombre_prueba character varying NOT NULL UNIQUE,
  descripcion text,
  instrucciones text,
  preguntas jsonb NOT NULL,
  configuracion_calificacion jsonb,
  activo boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  imagen_url text,
  CONSTRAINT catalogo_pruebas_pkey PRIMARY KEY (id)
);
CREATE TABLE public.evaluaciones_psicologicas (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  paciente_id uuid NOT NULL,
  cita_id uuid,
  psicologo_id uuid NOT NULL,
  prueba_id uuid NOT NULL,
  fecha_aplicacion timestamp with time zone DEFAULT now(),
  estado character varying DEFAULT 'Completado'::character varying CHECK (estado::text = ANY (ARRAY['Pendiente'::character varying::text, 'Completado'::character varying::text, 'Interpretado'::character varying::text])),
  respuestas_paciente jsonb NOT NULL,
  puntajes_brutos jsonb,
  puntajes_estandarizados jsonb,
  interpretacion_psicologo text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT evaluaciones_psicologicas_pkey PRIMARY KEY (id),
  CONSTRAINT eval_paciente_fkey FOREIGN KEY (paciente_id) REFERENCES public.pacientes(id_paciente),
  CONSTRAINT eval_cita_fkey FOREIGN KEY (cita_id) REFERENCES public.citas(id),
  CONSTRAINT eval_psicologo_fkey FOREIGN KEY (psicologo_id) REFERENCES public.empleados(id),
  CONSTRAINT eval_prueba_fkey FOREIGN KEY (prueba_id) REFERENCES public.catalogo_pruebas(id)
);
CREATE TABLE public.permisos_informes_psicologicos (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  tramite_id uuid NOT NULL,
  solicitante_id uuid NOT NULL,
  solicitante_nombre character varying NOT NULL,
  propietario_id uuid NOT NULL,
  propietario_nombre character varying NOT NULL,
  paciente_nombre character varying NOT NULL,
  estado character varying NOT NULL DEFAULT 'pendiente'::character varying CHECK (estado::text = ANY (ARRAY['pendiente'::character varying, 'aprobado'::character varying, 'denegado'::character varying]::text[])),
  duracion_horas integer DEFAULT 48,
  fecha_expiracion timestamp with time zone,
  created_at timestamp with time zone DEFAULT now(),
  responded_at timestamp with time zone,
  CONSTRAINT permisos_informes_psicologicos_pkey PRIMARY KEY (id),
  CONSTRAINT permisos_informes_psicologicos_tramite_id_fkey FOREIGN KEY (tramite_id) REFERENCES public.tramites_documentales(id),
  CONSTRAINT permisos_informes_psicologicos_solicitante_id_fkey FOREIGN KEY (solicitante_id) REFERENCES public.empleados(id),
  CONSTRAINT permisos_informes_psicologicos_propietario_id_fkey FOREIGN KEY (propietario_id) REFERENCES public.empleados(id)
);
CREATE TABLE public.landing_configuracion (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  mostrar_nosotros boolean DEFAULT true,
  mostrar_personal boolean DEFAULT true,
  mostrar_testimonios boolean DEFAULT true,
  mostrar_faq boolean DEFAULT true,
  updated_at timestamp with time zone DEFAULT timezone('utc'::text, now()),
  lock_key smallint NOT NULL DEFAULT 1 UNIQUE,
  CONSTRAINT landing_configuracion_pkey PRIMARY KEY (id)
);
CREATE TABLE public.landing_nosotros (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  mision_subtitulo character varying DEFAULT 'Comprometidos con tu bienestar emocional'::character varying,
  mision_texto text DEFAULT 'Ofrecer y brindar servicios psicológicos dirigidos a estudiantes, instituciones y comunidades, promoviendo la salud mental en nuestra sociedad a través de una atención responsable, ética y comprometida con el bienestar de los pacientes en todas las áreas.'::text,
  vision_subtitulo character varying DEFAULT 'Líderes en salud mental y desarrollo humano'::character varying,
  vision_texto text DEFAULT 'Ser una institución destacada y reconocida en el ámbito de la salud mental y el apoyo a instituciones educativas, contribuyendo a la prevención, promoción e intervención psicológica con calidad humana, ética profesional y compromiso social.'::text,
  imagen_url text DEFAULT 'https://placehold.co/600x400/007bff/ffffff?text=CEPSITCED+Nosotros'::text,
  creado_at timestamp with time zone DEFAULT timezone('utc'::text, now()),
  updated_at timestamp with time zone DEFAULT timezone('utc'::text, now()),
  lock_key smallint NOT NULL DEFAULT 1 UNIQUE,
  CONSTRAINT landing_nosotros_pkey PRIMARY KEY (id)
);
CREATE TABLE public.recordatorios_configuracion (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  habilitado boolean NOT NULL DEFAULT false,
  horas_antes integer NOT NULL DEFAULT 2,
  updated_at timestamp with time zone DEFAULT timezone('utc'::text, now()),
  CONSTRAINT recordatorios_configuracion_pkey PRIMARY KEY (id)
);
CREATE TABLE public.citas_historial_motivos (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  cita_id uuid NOT NULL,
  tipo character varying NOT NULL CHECK (tipo::text = ANY (ARRAY['Reprogramacion'::text, 'Cancelacion'::text, 'Exoneracion'::text])),
  motivo text NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT citas_historial_motivos_pkey PRIMARY KEY (id),
  CONSTRAINT citas_historial_motivos_cita_id_fkey FOREIGN KEY (cita_id) REFERENCES public.citas(id)
);
CREATE TABLE public.paquetes_catalogo_tramites_incluidos (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  paquete_catalogo_id uuid NOT NULL,
  servicio_tramite_id uuid NOT NULL,
  cantidad integer NOT NULL DEFAULT 1,
  created_at timestamp with time zone DEFAULT timezone('utc'::text, now()),
  CONSTRAINT paquetes_catalogo_tramites_incluidos_pkey PRIMARY KEY (id),
  CONSTRAINT pcti_paquete_fkey FOREIGN KEY (paquete_catalogo_id) REFERENCES public.paquetes_catalogo(id),
  CONSTRAINT pcti_servicio_fkey FOREIGN KEY (servicio_tramite_id) REFERENCES public.servicios(id)
);
CREATE TABLE public.paquetes_adquiridos_tramites_incluidos (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  paquete_adquirido_id uuid NOT NULL,
  servicio_tramite_id uuid NOT NULL,
  nombre_tramite_snapshot character varying,
  cantidad_total integer NOT NULL DEFAULT 1,
  cantidad_usada integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone DEFAULT timezone('utc'::text, now()),
  CONSTRAINT paquetes_adquiridos_tramites_incluidos_pkey PRIMARY KEY (id),
  CONSTRAINT pati_paquete_adquirido_fkey FOREIGN KEY (paquete_adquirido_id) REFERENCES public.paquetes_adquiridos(id),
  CONSTRAINT pati_servicio_fkey FOREIGN KEY (servicio_tramite_id) REFERENCES public.servicios(id)
);
CREATE TABLE public.cambios_correo_pendientes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  perfil_id uuid NOT NULL,
  correo_anterior character varying,
  correo_nuevo character varying NOT NULL,
  token_hash character varying NOT NULL UNIQUE,
  confirmado boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone DEFAULT now(),
  expira_en timestamp with time zone NOT NULL,
  CONSTRAINT cambios_correo_pendientes_pkey PRIMARY KEY (id),
  CONSTRAINT ccp_perfil_fkey FOREIGN KEY (perfil_id) REFERENCES public.perfiles(id)
);
CREATE TABLE public.documentos_administrativos (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  nombre text NOT NULL,
  categoria text NOT NULL CHECK (categoria = ANY (ARRAY['Contrato'::text, 'Factura'::text, 'Acta'::text, 'Certificado'::text, 'Otro'::text])),
  descripcion text,
  ruta_archivo text NOT NULL,
  tipo_mime text,
  tamanio_bytes bigint,
  subido_por uuid,
  fecha_subida timestamp with time zone NOT NULL DEFAULT now(),
  nombre_original text,
  CONSTRAINT documentos_administrativos_pkey PRIMARY KEY (id),
  CONSTRAINT documentos_administrativos_subido_por_fkey FOREIGN KEY (subido_por) REFERENCES public.empleados(id)
);
CREATE TABLE public.recuperaciones_password_pendientes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  empleado_id uuid NOT NULL,
  token_hash text NOT NULL UNIQUE,
  usado boolean NOT NULL DEFAULT false,
  expira_en timestamp with time zone NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT recuperaciones_password_pendientes_pkey PRIMARY KEY (id),
  CONSTRAINT recuperaciones_password_pendientes_empleado_id_fkey FOREIGN KEY (empleado_id) REFERENCES public.empleados(id)
);
CREATE TABLE public.tokens_verificacion (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  perfil_id uuid NOT NULL,
  token_hash character varying NOT NULL UNIQUE,
  created_at timestamp with time zone DEFAULT now(),
  expira_en timestamp with time zone NOT NULL,
  CONSTRAINT tokens_verificacion_pkey PRIMARY KEY (id),
  CONSTRAINT tv_perfil_fkey FOREIGN KEY (perfil_id) REFERENCES public.perfiles(id)
);
CREATE TABLE public.tokens_independizacion (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  paciente_id uuid NOT NULL,
  token_hash character varying NOT NULL UNIQUE,
  created_at timestamp with time zone DEFAULT now(),
  expira_en timestamp with time zone NOT NULL,
  CONSTRAINT tokens_independizacion_pkey PRIMARY KEY (id),
  CONSTRAINT ti_paciente_fkey FOREIGN KEY (paciente_id) REFERENCES public.pacientes(id_paciente)
);